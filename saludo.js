function saludar(name){
  return `Hola, ${name}`;
}

//exportar modulos
//primero el nombre que le queremos asignar a la propiedad = luego el nombre que lo asocia con la definicion de la funcion. Se puede asignar cualquier otro nombre la principio, pero es util y preferible asignarle el nombre de la funcion con la cual se va a importar, o sea que tengan el mismo

module.exports,saludar = saludar;
